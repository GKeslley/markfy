import React from 'react';
import styles from '../../Css/ReusablesCss/RequiredInput.module.css';

const RequiredInput = () => {
  return <span className={styles.requiredInput}>*</span>;
};

export default RequiredInput;
