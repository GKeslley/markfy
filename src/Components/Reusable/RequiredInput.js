import React from 'react';
import styles from '../../Css/ReusablesCss/RequiredInput.module.css';

const RequiredInput = () => {
  return <span className={styles['required-input']}>*</span>;
};

export default RequiredInput;
